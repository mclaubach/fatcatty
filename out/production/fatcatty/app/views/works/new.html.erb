<% if admin_signed_in? %>
<div id="newwork">
<div class="row text-center">
  <%= form_for(Work.new) do |f| %>
    <div>
      <%= f.text_field :title, placeholder: "title", size: '40' %>
    </div>
    <div style="padding-top:16px;">
      <%= f.file_field :image %>
    </div>
    <div style="padding-top:16px;">
      <%= f.submit "s u b m i t", class: "bigButton"%>
    </div>
  <% end %>
</div>
</div>
<% else %>
<p>get out of here, go away</p>
<% end %>
