  <% if admin_signed_in? %>
   <%= render partial: "works/new" %>
   <% else %>
  <% end %>
  <div class="inline-list" style="width:325px;">
    <% @works.each do |work| %>
    <%= work.title %>
    <%= link_to image_tag(work.image), work %>
  <% end %>
</div>
